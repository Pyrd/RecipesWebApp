export const actions = {
    async nuxtServerInit({ commit, }, { req, $vuetify }) {
        // const cookies = req.headers.cookie?.split(';')
        // if (cookies) {
        //     const jwt = cookies.find(() => "jwt")
        //     if (jwt) {
        //         // console.log(`Jwt found: ${jwt}`)
        //         dispatch('auth/initUser')
        //     }
        // }
        // commit('app/initTheme', $vuetify)

    }
}